Fs = 8;                     % Frecuencia de muestreo
N = 160;                    % Número de muestras
t = (0:N-1)/Fs;             % Tiempo

humedad_base = 50;
signal = humedad_base ...
       + 15*sin(2*pi*0.05*t) ...
       + 2*randn(1,N);

signal = max(0, min(100, signal));

save('synthetic_data_example.mat', 'signal', 'Fs', 't');
